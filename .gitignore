# Google Drive litter — this working copy lives on a synced drive
desktop.ini
.tmp.drivedownload/
.tmp.driveupload/

# Google Docs/Sheets shortcut stubs: 184-byte pointers, meaningless in git
*.gdoc
*.gsheet
*.gslides

# Superseded local backup of the Apps Script, kept on disk but out of the repo.
# The current version is apps-script/Code.gs; delete this when you're done with it.
apps-script/Code_old_v01.gs

# Participant credential lists produced by tools/new-roster.ps1. These are the
# only copy of the passwords — Supabase stores them hashed and cannot show them
# again — and they must never reach a public repository.
*roster*.csv
*.roster.csv

# Local-only roster for offline work (authMode "roster"). Never committed:
# this repository is public, so credentials in it are credentials in public.
# Copy content/roster.example.json to content/roster.json when you need it.
content/roster.json

# Editor and OS
.vscode/
.idea/
Thumbs.db
*.swp
